import { day1part1 } from ".";

describe("part 1", () => {
  it("should return 0 when some_test_input", () => {
    const testInput = `some_test_input`;

    const result = day1part1(testInput);

    expect(result).toBe(0);
  });
});
