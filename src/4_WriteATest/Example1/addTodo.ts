// import { randomUUID } from "crypto";

interface Todo {
  id: string;
  text: string;
  completed: boolean;
}

const addTodo1 = (currentTodos: Todo[], newTodo: { text: string }): Todo[] => {
  return [];
};
